{
  "name": "blog-engine",
  "version": "1.0.0",
  "description": "A modern full-stack blog application",
  "scripts": {
    "setup": "node setup.js && npm run install:all",
    "install:all": "npm run install:server && npm run install:client",
    "install:server": "cd server && npm install",
    "install:client": "cd client && npm install",
    "dev": "docker compose up -d && npm run dev:server & npm run dev:client",
    "dev:server": "cd server && npx prisma migrate dev && npm run dev",
    "dev:client": "cd client && npm run dev"
  }
}
